---
title: "Brasil IO"
output:
  html_document:
    df_print: paged
---

[API Dataset covid19 no Brasil.IO](https://github.com/turicas/covid19-br/blob/master/api.md)

```{r}
download_brasilio_table <- function(dataset, table_name){
  url <- sprintf("https://data.brasil.io/dataset/%s/%s.csv.gz", dataset, table_name)
  tmp <- tempfile()
  download.file(url, tmp)
  response <- read.csv(gzfile(tmp), encoding = "UTF-8")
  unlink(tmp)
  return(response)
}

# Passe o nome da tabela para a funcao, como "caso", "caso_full", "obito_cartorio":
data <- download_brasilio_table("covid19", "caso_full")
```
```{r}
library(dplyr)
y21 <- data %>%
  select(state, date, last_available_death_rate) %>%
  filter(date >= "2021-01-01" & date <= "2021-01-02")
head(y21)
```


